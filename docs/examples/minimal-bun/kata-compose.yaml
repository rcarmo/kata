# Minimal Bun example for Kata
# - Serves a tiny JSON endpoint via Bun
# - Traefik labels are generated automatically; host defaults to <app>.localhost

environment:
  PORT: "8000"

services:
  web:
    runtime: bun
    command: bun run index.ts
    expose:
      - "${PORT}"
